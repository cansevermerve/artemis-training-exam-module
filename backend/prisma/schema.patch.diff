--- a/backend/prisma/schema.prisma
+++ b/backend/prisma/schema.prisma
@@ -36,6 +36,7 @@
   assignedByRecords TrainingAssignment[] @relation("AssignmentCreator")
   trainingDocuments TrainingDocument[]   @relation("DocumentEmployee")
   uploadedDocuments TrainingDocument[]   @relation("DocumentUploader")
+  examResultAudits ExamResultAudit[]      @relation("ExamResultAuditEditor")
 
   @@map("User")
 }
@@ -64,6 +65,7 @@
   shuffleQuestions        Boolean   @default(false)
   shuffleOptions          Boolean   @default(false)
   showCorrectAnswers      Boolean   @default(false)
+  coverImageUrl           String?
   createdById             String
   createdAt               DateTime  @default(now())
   updatedAt               DateTime  @updatedAt
@@ -89,6 +91,9 @@
   startedAt          DateTime?
   contentCompletedAt DateTime?
   completedAt        DateTime?
+  cancelledAt        DateTime?
+  cancellationReason String?
+  status             TrainingAssignmentStatus @default(ASSIGNED)
   createdAt          DateTime  @default(now())
   updatedAt          DateTime  @updatedAt
 
@@ -104,6 +109,15 @@
   @@index([assignedById])
 }
 
+enum TrainingAssignmentStatus {
+  ASSIGNED
+  IN_PROGRESS
+  COMPLETED
+  FAILED
+  EXPIRED
+  CANCELLED
+}
+
 enum TrainingContentType {
   VIDEO
   PDF
@@ -123,10 +137,11 @@
   title       String
   fileUrl     String?
   externalUrl String?
-  order       Int
-  isRequired  Boolean             @default(true)
-  createdAt   DateTime            @default(now())
-  updatedAt   DateTime            @updatedAt
+  order           Int
+  isRequired      Boolean             @default(true)
+  durationSeconds Int?
+  createdAt       DateTime            @default(now())
+  updatedAt       DateTime            @updatedAt
 
   training Training                  @relation(fields: [trainingId], references: [id], onDelete: Cascade)
   progress TrainingContentProgress[]
@@ -158,7 +173,8 @@
 model QuestionOption {
   id         String  @id @default(uuid())
   questionId String
-  text       String
+  text       String?
+  imageUrl   String?
   order      Int
   isCorrect  Boolean @default(false)
 
@@ -202,15 +218,23 @@
   status          ExamAttemptStatus @default(IN_PROGRESS)
   startedAt       DateTime          @default(now())
   submittedAt     DateTime?
+  expiresAt       DateTime?
   durationSeconds Int?
+  totalPoints     Int?
   score           Int?
   passed          Boolean?
+  correctCount    Int?
+  wrongCount      Int?
+  unansweredCount Int?
+  questionOrder   Json?
+  optionOrder     Json?
   createdAt       DateTime          @default(now())
   updatedAt       DateTime          @updatedAt
 
   assignment TrainingAssignment @relation(fields: [assignmentId], references: [id], onDelete: Cascade)
   answers    ExamAnswer[]
   documents  TrainingDocument[]
+  resultAudits ExamResultAudit[]
 
   @@unique([assignmentId, attemptNumber])
   @@index([assignmentId])
@@ -218,12 +242,41 @@
 }
 
 
+model ExamResultAudit {
+  id                      String            @id @default(uuid())
+  attemptId               String
+  editedById              String
+  reason                  String
+  previousStatus          ExamAttemptStatus
+  newStatus               ExamAttemptStatus
+  previousScore           Int?
+  newScore                Int
+  previousPassed          Boolean?
+  newPassed               Boolean
+  previousCorrectCount    Int?
+  newCorrectCount         Int
+  previousWrongCount      Int?
+  newWrongCount           Int
+  previousUnansweredCount Int?
+  newUnansweredCount      Int
+  previousAnswers         Json
+  newAnswers              Json
+  createdAt               DateTime          @default(now())
+
+  attempt  ExamAttempt @relation(fields: [attemptId], references: [id], onDelete: Cascade)
+  editedBy User        @relation("ExamResultAuditEditor", fields: [editedById], references: [id])
+
+  @@index([attemptId, createdAt])
+  @@index([editedById])
+}
+
 model ExamAnswer {
   id            String   @id @default(uuid())
   attemptId     String
   questionId    String
-  isCorrect     Boolean
+  isCorrect     Boolean?
   earnedPoints  Int      @default(0)
+  answeredAt    DateTime?
   createdAt     DateTime @default(now())
   updatedAt     DateTime @updatedAt
 
@@ -254,6 +307,12 @@
   ATTENDANCE_FORM
   SIGNED_ATTENDANCE_FORM
   OSGB_CERTIFICATE
+  TRAINING_COVER
+  TRAINING_CONTENT
+  QUESTION_IMAGE
+  OPTION_IMAGE
+  PARTICIPANT_LIST
+  RESULTS_REPORT
   OTHER
 }
 
@@ -266,11 +325,11 @@
 
 model TrainingDocument {
   id           String                 @id @default(uuid())
-  employeeId   String
+  employeeId   String?
   trainingId   String
   assignmentId String?
   attemptId    String?
-  uploadedById String
+  uploadedById String?
   type         TrainingDocumentType
   status       TrainingDocumentStatus @default(ARCHIVED)
   title        String
@@ -279,13 +338,14 @@
   mimeType     String
   sizeBytes    Int
   filePath     String
+  checksumSha256 String?
   documentDate DateTime?
   isGenerated  Boolean                @default(false)
   createdAt    DateTime               @default(now())
   updatedAt    DateTime               @updatedAt
 
-  employee   User                @relation("DocumentEmployee", fields: [employeeId], references: [id])
-  uploadedBy User                @relation("DocumentUploader", fields: [uploadedById], references: [id])
+  employee   User?               @relation("DocumentEmployee", fields: [employeeId], references: [id], onDelete: SetNull)
+  uploadedBy User?               @relation("DocumentUploader", fields: [uploadedById], references: [id], onDelete: SetNull)
   training   Training            @relation(fields: [trainingId], references: [id], onDelete: Cascade)
   assignment TrainingAssignment? @relation(fields: [assignmentId], references: [id], onDelete: SetNull)
   attempt    ExamAttempt?        @relation(fields: [attemptId], references: [id], onDelete: SetNull)
